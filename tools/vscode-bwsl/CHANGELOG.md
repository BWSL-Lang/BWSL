# Changelog

## Unreleased

- Add compiler-backed diagnostics using `bwslc -errors-json`.
- Use `bwslc -check --stdin --source-file` for diagnostics without output files.

## 0.1.0

- Initial BWSL language extension with TextMate grammar, language configuration, and snippets.
