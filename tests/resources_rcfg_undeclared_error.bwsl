// Test: resources declared only in legacy .rcfg are rejected when the pipeline has resources { }

pipeline ResourcesRcfgUndeclaredError {
    attributes {
        position: float3
    }

    resources {
        viewProj: mat4
    }

    pass "Main" {
        use attributes { position }
        use resources { viewProj }

        vertex {
            float4 p = resources.legacyMatrix * float4(attributes.position, 1.0);
            output.position = resources.viewProj * p;
        }

        fragment {
            output.color = float4(1.0);
        }
    }
}
