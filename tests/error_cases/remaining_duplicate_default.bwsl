// Regression: Duplicate switch default case.
pipeline Remaining {

    pass "Main" {
        vertex { output.position=float4(0.0,0.0,0.0,1.0); }
        fragment { int n=1; switch(n) { default: n=2; default: n=3; } output.color=float4(1.0); }
    }
}
