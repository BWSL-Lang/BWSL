pipeline ModuleInsidePipeline {
    module Inner {
        identity :: (float x) -> float {
            return x;
        }
    }

    pass "Main" {
        compute "Main" [1, 1, 1] {
            uint idx = input.global_id.x;
        }
    }
}
