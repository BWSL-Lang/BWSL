pipeline InvalidReturn {
    invalidReturn :: (float x) -> float {
        return if (x < 0.0);
        return x * 2.0;
    }
    pass "Main" {
        vertex { output.position = float4(invalidReturn(1.0)); }
    }
}
