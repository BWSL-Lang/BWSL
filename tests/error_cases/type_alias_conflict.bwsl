pipeline TypeAliasConflict {
    import PBR as BRDF
    using Material = BRDF::PBRMaterial
    using Material = BRDF::PBRMaterial

    pass "Main" {
        compute "Main" [1, 1, 1] {
            uint idx = input.global_id.x;
        }
    }
}
