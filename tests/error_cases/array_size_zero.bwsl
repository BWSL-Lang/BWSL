// Error case: array sizes must be positive.
pipeline ArraySizeNotPositive {
    attributes {
        position: float3
    }

    pass "Main" {
        use attributes { position }

        vertex {
            float[0] values;
            output.position = float4(attributes.position, 1.0);
        }
    }
}
