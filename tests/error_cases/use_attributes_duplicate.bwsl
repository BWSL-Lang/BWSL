// Error case: 'use attributes' entries must be unique.
pipeline WrongAcceptUseAttributesDuplicate {
    attributes {
        position: float3
    }

    pass "Main" {
        use attributes { position, position }

        vertex {
            output.position = float4(attributes.position, 1.0);
        }
    }
}
