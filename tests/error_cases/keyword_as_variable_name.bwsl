// Error case: language keywords cannot be used as local variable names.
pipeline KeywordAsVariableName {
    attributes {
        position: float3
    }

    pass "Main" {
        use attributes { position }

        vertex {
            int pass = 1;
            output.position = float4(attributes.position, float(pass));
        }
    }
}
