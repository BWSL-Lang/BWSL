submodule MissingParentExtension extends DoesNotExist {
    value :: (float x) -> float {
        return x;
    }
}

pipeline SubmoduleExtendsUnknownParent {
    pass "Main" {
        vertex {
            output.position = float4(0.0, 0.0, 0.0, 1.0);
        }

        fragment {
            output.color = float4(1.0);
        }
    }
}
